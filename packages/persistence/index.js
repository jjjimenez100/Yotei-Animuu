const Repository = require('./lib/Repository');
const ScheduledDownloadEntity = require('./lib/ScheduledDownloadEntity');

// TODO: CLI app and integrations

module.exports = {
  Repository,
  ScheduledDownloadEntity,
};